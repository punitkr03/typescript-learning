enum seating {
    AISLE,
    MIDDLE,
    WINDOW
}

const choice = seating.MIDDLE;

/*The above code generates a lot of JS so we can make the enum const to generate only generate code
which is absolutelt necessary.*/

export{}